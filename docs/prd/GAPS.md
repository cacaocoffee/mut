# 갭 대장

PRD를 코드·시안·SPEC과 대조해 나온 **누락(빠짐)** 과 **충돌(서로 다른 말)** 목록.
결정이 끝난 것만 PRD 원문에 반영하고 버전을 올린다. 현재 PRD는 **v1.2**.

- **충돌**은 둘 중 하나가 반드시 틀렸다는 뜻이다. 방치하면 코드가 조용히 한쪽을 고른다.
- **심각도 A**는 지금 구현을 막거나 이미 어긋나 있는 것. B는 Phase 1 착수 전 필요. C는 그 이후.

---

## 요약

| ID | 구분 | 심각도 | 한 줄 |
|---|---|---|---|
| [G-01](#g-01) | 충돌 | ✅ 해결 | 당도 단계 수 — **4단계로 확정** (ADR-0002) |
| [G-02](#g-02) | 충돌 | ✅ 해결 | 향·맛 태그 — **합집합 10개로 확정** (ADR-0002) |
| [G-03](#g-03) | 누락 | ✅ 해결 | 스타일 축 **도입 + 24종 색인 완료** (ADR-0002) |
| [G-04](#g-04) | 충돌 | ✅ 해결 | 기주 슬러그 — **`soju` → `korean`** 등 정리 (ADR-0002) |
| [G-05](#g-05) | 충돌 | ✅ 해결 | 부록 A.1·A.2·A.6 **폐기, Modernist 채택** (ADR-0001) |
| [G-06](#g-06) | 충돌 | ✅ 해결 | **라이트 단일 테마**, NFR 수정 (ADR-0001) |
| [G-07](#g-07) | 누락 | ✅ 해결 | **PRD §17 기술 스택 신설.** 단 DB·CMS·호스팅은 여전히 미정 |
| [G-08](#g-08) | 충돌 | ✅ 해결 | 도수 필터 — **PRD 4구간으로 통일** (ADR-0003) |
| [G-09](#g-09) | 누락 | ◐ 진행 | SCREENS-00·01·03·06 작성. **02·04·05 남음** (전부 Phase 1b~2) |
| [G-10](#g-10) | 누락 | ✅ 해결 | [SPEC-10](../spec/SPEC-10_계측_이벤트.md) — 이벤트 10종 · 지표 8개 매핑 |
| [G-11](#g-11) | 누락 | ✅ 해결 | [SPEC-08](../spec/SPEC-08_보안_권한_개인정보.md) — 역할 4종 · 권한 매트릭스 · 스코프 |
| [G-12](#g-12) | 누락 | ✅ 해결 | 성인 인증 — **전면 게이트 폐기**로 항목 자체가 소멸 (ADR-0004) |
| [G-13](#g-13) | 누락 | ✅ 해결 | Postgres `pg_trgm` + 초성 색인 컬럼 (SPEC-06 §5) |
| [G-14](#g-14) | 누락 | ✅ 해결 | [SPEC-07 API명세](../spec/SPEC-07_API명세.md) 작성 |
| [G-15](#g-15) | 누락 | ✅ 해결 | [SCREENS-06](../screens/SCREENS-06_어드민.md) — 발행 조건 패널 · 입력 강제 |
| [G-16](#g-16) | 누락 | ✅ 해결 | [SPEC-04](../spec/SPEC-04_비기능요구사항.md) — 대비 실측표 + 위반 9곳 수정 |
| [G-17](#g-17) | 미결 | C | PRD 16장 결정 필요 사항 10건 — 사업 결정 (**에디터 채용 형태는 해소**) |
| [G-18](#g-18) | 오류 | ✅ **해결** | TRACE-00 §4.1 신설로 정정. 대응 확정 — 031 API만 · 041 SCREENS-03 문서화 |
| [G-19](#g-19) | 누락 | ✅ **해결** | SPEC-06에 `verification_task` · `category_intro` · `cocktail.flavor_profile` 추가 |
| [G-20](#g-20) | 충돌 | ✅ **해결** | `FR-ADMIN-007` 문구를 "관리자 승인"으로 · `NFR-S-07`을 경고로 (SPEC-04 §3.1) |
| [G-21](#g-21) | 확인 | ✅ **해결** | `batch_run` 제거(근거 없음) · `idempotency_key` 유지(SPEC-07 §1.7 요구) |
| [G-22](#g-22) | 개정 | ✅ **해결** | **`NFR-L-05` 트리거 분해** — 법률 검토가 개발·오픈 차단이 아니게 됨 (SPEC-04 §8.1) |

---

## 진행 상황 — 2026-08-06

**해결 12건** — A등급 7건 + [G-08](#g-08) · [G-11](#g-11) · [G-12](#g-12) · [G-13](#g-13) · [G-14](#g-14).

ADR [0001](../decisions/ADR-0001-design-system.md) · [0002](../decisions/ADR-0002-taxonomy.md) ·
[0003](../decisions/ADR-0003-graph-source-and-abv-bands.md) · [0004](../decisions/ADR-0004-age-gate.md)로
결정하고 PRD v1.2와 SPEC-00~07에 반영했다.

아래 본문은 **당시의 문제 진술과 근거**로 남긴다 — 왜 그렇게 정했는지가 결정 자체보다 오래 쓸모 있다.

**남은 것 2건**

| ID | 무엇 | 언제 |
|---|---|---|
| [G-09](#g-09) | 화면 명세 — SCREENS 02·04·05 | 바 도메인 착수 시 |
| [G-17](#g-17) | PRD 16장 미결 10건 | 사업 결정 |

문서로 해소되지 않는 것은 **호스팅 · 이미지 저장소 미정** 하나다 ([G-07](#g-07) 하단).
이건 사업/인프라 결정이라 스펙을 더 써도 풀리지 않는다.

---

## 진행 상황 — 2026-08-07 (Phase 1a 이슈 분해)

[`docs/issues/`](../issues/INDEX.md) 52개를 작성하며 스펙을 전수 대조해 **G-18~G-22를 발견하고 전부 해소**했다.

| 무엇 | 결과 |
|---|---|
| SPEC-06 | `verification_task` · `category_intro` · `cocktail.flavor_profile` 추가 |
| SPEC-03 | `FR-ADMIN-007` 문구 — "에디터 승인" → **"에디터가 요청하고 관리자가 승인"** |
| SPEC-04 | `NFR-S-07` 발행 차단 → **경고** (§3.1) · **`NFR-L-05` 트리거 분해** (§8.1) |
| TRACE-00 | §4.1 신설 — "1a 화면 명세 완료" 결론 정정. G3·G7 판정 수정 |

**남은 것**

| ID | 무엇 | 성격 |
|---|---|---|
| [G-07](#g-07) 하단 | 호스팅 · 이미지 저장소 | 사업/인프라 — 이슈 044·045만 제한 |
| [G-09](#g-09) | SCREENS 02·04·05 | 1b. **1a는 대응 확정**(031 API만 · 041 문서화) |
| [G-16](#g-16) 잔여 | `.btn-primary` 대비 3.76:1 | 제품 결정 — 이슈 050 `BLOCKED` |
| [G-17](#g-17) | 사업 결정 9건 | 에디터 채용 형태는 해소 |

**착수를 막는 것은 없다.** 미결은 특정 이슈만 제한한다.
구현 중 미결은 [`docs/issues/DECISIONS.md`](../issues/DECISIONS.md)가 관리한다 — 원칙 이탈만 여기 온다 (SPEC-00 §4).

---

## A등급 — 해결됨 (기록)

### G-01

**당도 단계 수 충돌**

| 출처 | 값 |
|---|---|
| PRD `R-F1.1-5` (267줄) | 3단계 — 드라이 · 미디엄 · 스위트 |
| PRD 데이터 모델 (457줄) | `sweetness enum dry \| medium \| sweet` |
| 시안 · 현재 코드 | 4단계 — 드라이 · 세미 드라이 · 세미 스위트 · 스위트 |

현재 `packages/domain/src/types.ts`의 `SweetLevel`은 `0|1|2|3`이고, 24개 항목이 전부 4단계로 색인돼 있다.
파인더 점수 계산(`matchScore`)도 4단계 전제로 `sweet === 1 ? 1.5 : sweet` 같은 보정을 쓴다.

> **제안: 4단계 유지.** 3단계로 줄이면 시안의 당도 태그 색 스케일(중립 → accent-2 → accent-200 → accent 채움)이
> 4칸 전제라 다시 디자인해야 하고, 24개 항목의 재색인이 필요하다. 무엇보다 "세미 드라이"와 "세미 스위트"를
> 하나로 합치면 네그로니(0)와 김렛(1)이 같은 칸에 들어가는데, 이 둘은 실제로 다른 음료다.
> PRD 쪽을 4단계로 고치는 편이 싸다.

**차단 대상** — 데이터 모델 확정, 필터 UI, 파인더 점수식.

---

### G-02

**향·맛 태그 집합 충돌**

| PRD 6.3 (9개) | 시안 · 코드 (7개) |
|---|---|
| 시트러스 | `citrus` ✅ |
| 프루티 | `fruity` ✅ |
| 허브 | `herbal` ✅ |
| 스파이시 | `spicy` ✅ |
| 스모키 | `smoky` ✅ |
| 비터 | `bitter` ✅ |
| 플로럴 | — **없음** |
| 너티 · 로스티 | — **없음** |
| 크리미 | — **없음** |
| — | `sour` **시안에만 있음** |

PRD는 산미를 시트러스 안에 넣었다("레몬·라임·자몽 계열의 산미"). 시안은 `citrus`(향)와 `sour`(맛)를 분리했다.
현재 코드의 `matchScore`는 이 분리를 전제로 `citrus`를 고르면 `sour`에 부분 점수(+14)를 준다.

> **제안: 시안 7개 + PRD 3개 = 9개로 합집합.** `sour`를 살리고 `플로럴 / 너티·로스티 / 크리미`를 추가한다.
> 근거는 두 가지다. (1) `sour`와 `citrus`는 실제로 다른 축이다 — 위스키 사워는 `sour`지만 시트러스 향이 주인공이 아니다.
> (2) 빠진 3개는 커버리지 문제다. 에스프레소 마티니에 `너티·로스티`가 없으면 "커피 향" 검색이 불가능하고,
> 24종을 500종으로 늘리면 플로럴(엘더플라워·바이올렛) 계열이 반드시 들어온다.
>
> 단, `R-F1.2-1`의 **최대 3개 제한**은 유지한다. 현재 데이터는 최대 3개라 그대로 통과한다.

**차단 대상** — 필터 UI, 파인더 3번 질문, 500종 확장 시 재색인 비용.

---

### G-03

**스타일 축이 데이터에 없음 — `R-C-1` 위반**

PRD 5장은 카테고리를 3축(기주 · 스타일 · 메이킹 방법)으로 정의하고, `R-C-1`에서
"세 축은 모든 칵테일에 필수값이다. NULL을 허용하면 카테고리 페이지에서 누락이 생긴다"고 못박았다.
`R-C-3`의 배리에이션 추천도 `style_primary` 일치를 1순위로 쓴다.

그런데 시안에도, 현재 `packages/domain/src/data.ts`에도 **스타일 필드가 없다.**
현재 상세 화면의 "같은 기주 RELATED"는 기주만으로 추천하는데, 이건 `R-C-3`가 명시적으로
"NULL이면 추천이 태그 매칭으로 퇴화한다"고 경고한 바로 그 상태다.

> **제안: `style[]` + `stylePrimary` 필드를 추가하고 24종을 색인한다.** PRD 5.2의 9종 중 현재 데이터가
> 실제로 쓰는 것은 6종(`highball` `sour` `spirit-forward` `tiki` `creamy` — `spritz` `hot` `frozen` `shot`은 해당 없음)이다.
> 24개 색인은 한 번에 끝나고, 이걸 미루면 500종 시점에 전수 재색인이 된다.
>
> 색인 초안(제안):
>
> | 스타일 | 해당 항목 |
> |---|---|
> | `spirit-forward` | 네그로니 · 마티니 · 올드패션드 · 맨해튼 · 불바디에 · 문배 올드패션드 |
> | `sour` | 김렛 · 사우스사이드 · 코즈모폴리탄 · 위스키 사워 · 페니실린 · 다이키리 · 마르가리타 |
> | `highball` | 진토닉 · 모스코 뮬 · 블러디 메리 · 모히토 · 다크 앤 스토미 · 팔로마 · 소주 하이볼 · 시트러스 슈럽 |
> | `tiki` | 마이 타이 |
> | `creamy` | 에스프레소 마티니 · 막걸리 콜라다 |

**차단 대상** — 카테고리 URL(`/cocktails/style/sour/`), 배리에이션 추천, SEO 랜딩 페이지.

---

### G-04

**기주 값 집합 충돌**

| PRD 5.1 (10종) | 시안 · 코드 (7종) |
|---|---|
| `gin` 진 | 진 ✅ |
| `vodka` 보드카 | 보드카 ✅ |
| `rum` 럼 | 럼 ✅ |
| `whisky` 위스키 | 위스키 ✅ |
| `agave` 데킬라 · 메즈칼 | **데킬라** — 메즈칼 누락 |
| `soju` 소주 · 막걸리 | **전통주** — 이름 불일치, 범위 더 넓음 |
| `non-alcoholic` 논알콜 | **무알콜** — 이름 불일치 |
| `brandy` 브랜디 | 없음 |
| `liqueur` 리큐르 | 없음 |
| `wine` 와인 · 스파클링 | 없음 |

없는 3종은 지금 데이터에 해당 항목이 없어서일 뿐 충돌은 아니다. 진짜 문제는 **이름 3건**이다.
`전통주`는 문배주·소주·막걸리를 다 담는데 PRD 슬러그는 `soju`다. 이건 URL(`/cocktails/base/soju/`)이 되므로
지금 정하지 않으면 나중에 리다이렉트를 깔아야 한다.

> **제안:** 표시명은 시안(`전통주` · `무알콜`), 슬러그는 PRD를 따르되 `soju` → **`korean`** 으로 바꾼다.
> 막걸리와 문배주를 `soju`로 부르는 건 부정확하고, PRD 5.1 스스로 이 축을 "국내판 정체성 축"이라 했다.
> `데킬라`는 표시명을 `데킬라 · 메즈칼`로 넓히고 슬러그는 `agave` 유지.

**차단 대상** — 카테고리 URL 확정, `BaseSpirit` 타입.

---

### G-05

**부록 A가 시안과 다름 — 문서 스스로 폐기를 예고함**

부록 A는 첫 줄에 이렇게 적혀 있다(622–624줄):

> 시안 미확인 — 기존 Claude Design 시안을 열람하지 못해 아래는 제안안이다.
> 시안 파일을 받으면 팔레트와 타이포를 그쪽 기준으로 교체한다.

시안은 확보됐다(`docs/design/source/`). 실제 값은 정반대에 가깝다.

| | 부록 A 제안 | 확보된 시안 (Modernist) |
|---|---|---|
| 그라운드 | 다크 `#121715` | **라이트 `#f3f2f2`** |
| 강조색 | 청자 `#4F8975` + 주칠 `#B03A2E` 2색 | **단색 `#ec3013`** |
| 제목 서체 | Gowun Batang (명조) | **Archivo (산세리프) 800** |
| 본문 서체 | Pretendard | **Archivo + Noto Sans KR** |
| 숫자 서체 | JetBrains Mono | 없음 — `font-variant-numeric: tabular-nums` |
| radius | 2px | **0px** ("어디에도 라운드를 주지 말 것") |
| Elevation | 그림자 없음 | `--shadow-sm/md/lg` 있음 |

> **제안: 부록 A.1 · A.2 · A.6을 폐기하고 Modernist로 교체.** 문서가 지시한 대로다.
> A.3~A.5(스페이싱 체계, 컴포넌트 목록, 레시피 스펙 카드 구성 규칙)는 **구조**라서 살린다 —
> 부록 A 머리말도 "구조는 시안과 무관하게 유효하다"고 했다.
> 다만 A.5의 색 지정(계량 `청자-300`, 당도 `주칠-500`)은 Modernist 토큰으로 치환한다.

**차단 대상** — 없음(코드는 이미 Modernist를 씀). 문서만 현실과 어긋나 있다.

---

### G-06

**다크 모드 충돌 — 남은 진짜 결정**

G-05를 "시안 우선"으로 정리해도 이건 안 풀린다. NFR(541줄)이 별도로 요구한다:

> 접근성 — 명도 대비 WCAG AA. **다크 모드가 기본 테마**

그런데 Modernist에는 다크 테마가 **정의돼 있지 않다.** `styles.css`의 램프는 라이트 그라운드
한 벌뿐이고, 시안 readme는 "a near-mono red on white"를 시스템의 정체성으로 규정한다.

선택지는 셋이고, 이건 제품 결정이라 제가 정하지 않았다.

| 안 | 내용 | 비용 |
|---|---|---|
| **가** | 라이트 단일. NFR의 "다크 기본"을 삭제 | 없음. 지금 상태 그대로 |
| **나** | 다크를 Modernist 규칙으로 파생 (accent를 `--color-accent-500`로 올리고 램프 반전) | 램프 재생성 + 전 컴포넌트 대비 재검증 |
| **다** | 라이트 기본 + 다크 옵션 | 나 + 토글·저장 |

> **제안: 가(라이트 단일), 단 근거를 남길 것.** "바는 어두우니 다크"는 부록 A의 논리였고,
> 그 부록은 시안 없이 쓰였다. 시안이 라이트를 골랐고 이미 구현·검증됐다.
> 다크가 정말 필요하다는 근거(사용자 조사·야간 사용 비중)가 나오면 그때 **나**로 간다.

**차단 대상** — `packages/ui` 토큰 확장 여부. 결정 전엔 다크 관련 코드를 쓰지 않는다.

---

### G-07

**기술 스택이 PRD에 없음**

PRD 768줄 어디에도 프레임워크 · DB · 호스팅 언급이 없다. NFR 12장이 "SSR 또는 SSG 필수",
"정적 생성 후 ISR", "어드민 CMS 필수"라고 요구 수준만 적었다.

현재 코드가 이미 고른 것:

| 층 | 선택 | 근거 |
|---|---|---|
| 프레임워크 | Next.js 16 (App Router) | NFR SSG + ISR 요구를 그대로 만족 |
| 언어 | TypeScript strict | — |
| 구조 | npm workspaces 모노레포 | `apps/web` · `packages/domain` · `packages/ui` |
| 스타일 | 순수 CSS + 토큰 | 시안이 플레인 CSS 디자인 시스템 |
| 데이터 | **없음 — 정적 TS 배열 24개** | 프로토타입 단계 |
| 호스팅 | 미정 | — |
| CMS | 미정 | NFR이 요구하나 후보 없음 |

> **제안:** 확정된 것은 PRD에 적어 굳히고, 미정 3건(DB · 호스팅 · CMS)은 16장 결정 목록에 올린다.
> 특히 **DB와 CMS는 G-14(API 계약)·G-15(어드민)를 막고 있어** Phase 1 착수 전 필수다.

**진행 (2026-08-06)** — DB(PostgreSQL 16) · CMS(자체 어드민) · 백엔드(Kotlin + Spring)가 확정됐고
[SPEC-05](../spec/SPEC-05_아키텍처.md) · [SPEC-06](../spec/SPEC-06_데이터모델_ERD.md) ·
[SPEC-07](../spec/SPEC-07_API명세.md)에 반영했다.

**남은 미정 2건**

| 항목 | 막고 있는 것 |
|---|---|
| **호스팅 · 배포** | 배포 파이프라인. **추가 제약** — httpOnly 쿠키 세션이라 프론트와 API가 같은 상위 도메인이어야 한다 ([SPEC-07 §1.2](../spec/SPEC-07_API명세.md)) |
| **이미지 저장소 · CDN** | 에디터의 발행 시작. 다만 `media_asset.storage_key`가 추상화 지점이라 **스키마는 바뀌지 않는다** |

---

## B등급 — Phase 1 착수 전

### G-08
✅ **해결 (2026-08-06)** — PRD 4구간을 정본으로 통일하고 슬라이더를 버렸다.
연속 슬라이더에는 값별 카운트를 붙일 수 없어 `R-F2.1-2`의 예외가 되기 때문이다. [ADR-0003](../decisions/ADR-0003-graph-source-and-abv-bands.md).

**코드 반영 완료** — `abvBandOf()`가 단일 정의이고 탐색 필터·파인더·패싯 카운트가 전부 이걸 쓴다.
`npm run check`가 구간 합계와 코퍼스 크기가 일치하는지(구멍이 없는지) 검증한다.

<details><summary>당시 문제 진술</summary>

PRD 7.1은 4구간(논알콜 / ~10% / 10–20% / 20%~), 시안은 0–45 연속 슬라이더 + 상한 필터.
파인더(4문항)는 또 다른 4구간(0 / ~14 / ~25 / 25%+)을 쓴다. **세 곳이 전부 다르다.**
</details>

### G-09
◐ **진행 중 (2026-08-06)** — [SCREENS-00 공통규칙](../screens/SCREENS-00_인덱스_공통규칙.md)과
[SCREENS-01 칵테일](../screens/SCREENS-01_칵테일.md) 작성.

상태 5종(정상·빈 결과·로딩·에러·권한 없음)의 표현 규칙을 공통으로 잡았다.
**권한 없음은 404로 표현한다** — `403`은 리소스의 존재를 알려준다.

**남은 것** — SCREENS-02(바 · Phase 1b) · 04(마이) · 05(파트너 · Phase 1b).

06(어드민)은 [G-15](#g-15)에서, **03(파인더)은 이슈 041에서** 썼다 —
설계가 아니라 **이미 있는 화면의 기록**이라 명세 없이 착수할 수 있었다 ([G-18](#g-18)의 판단).

### G-10
✅ **해결 (2026-08-06)** — [SPEC-10](../spec/SPEC-10_계측_이벤트.md).
Phase 1a 이벤트 7종 + 1b 3종, PRD 2.2 지표 8개 전부 계산식 매핑.

**1a에서 검증 가능한 지표는 셋뿐**임이 드러났다 — 등록 수 · MAU · 유기 검색 비중.
크로스 이동률은 `cross_nav`가 필요하고 그건 바가 있어야 한다.

가장 값진 신규 이벤트는 **`search_miss`** — 검색됐는데 0건인 검색어가
곧 수요가 확인된 콘텐츠 후보다. 에디터가 다음에 뭘 쓸지에 데이터로 답한다.

### G-11
✅ **해결 (2026-08-06)** — [SPEC-08](../spec/SPEC-08_보안_권한_개인정보.md).
역할 4종, 권한 매트릭스, `partner_owner` 스코프(`bar_owner` 검증 → 실패 시 404).

**노출 규칙은 `admin`도 못 바꾼다**를 매트릭스 마지막 줄에 명시했다 — API·DB 어디에도 없어서
코드를 고치고 배포해야만 바뀐다 (`PRIN-P02`).

부수 소득 — 이 문서를 쓰다가 **SPEC-06에 `bar_owner`와 `user_role`이 없다는 걸 발견**해 보강했다.
`partner_contract`는 바와 등급만 이었고 사용자 연결이 없어서 "자기 바"를 판정할 수 없었다.

### G-12
✅ **해결 (2026-08-06)** — 갭을 채우는 대신 **요구사항 자체를 폐기**했다.

`R-F5-4`의 "주류 콘텐츠 서비스로서 필수다"에 근거가 없었다. 주류를 팔지 않으므로
규제 접점은 브랜디드 콘텐츠 한 종류뿐이고, 전면 게이트는 SEO 목표와 정면 충돌하면서
생년월일 자가신고라 방어력도 없다. [ADR-0004](../decisions/ADR-0004-age-gate.md).

**법률 검토 대기 조건부다.** PRD 15장 검토 항목에 "전면 성인 인증이 실제로 요구되는가"를 넣었다.
검토 결과 필요하다고 나오면 게이트와 SEO 중 하나를 포기해야 한다.

<details><summary>당시 문제 진술</summary>

`R-F5-4`는 "최초 진입 시 생년월일 게이트"만 말한다. 재확인 주기, 통과 상태 저장 위치,
봇·크롤러 처리, SEO 영향 미정. 게이트가 SSG 페이지 앞에 서면 12장 SEO 요구와 충돌할 수 있다.
</details>

### G-13
✅ **해결 (2026-08-06)** — `search_document` 테이블에 초성 분해 컬럼(`chosung`)을 두고
`pg_trgm` + GIN으로 프리픽스 매칭한다. 저장 시점에 분해한다 — 조회 때 계산하면 인덱스가 안 붙는다.
별도 검색엔진은 코퍼스가 수천 건을 넘을 때 검토한다.
[SPEC-06 §3.8 · §5](../spec/SPEC-06_데이터모델_ERD.md).

**코드 반영은 아직이다** — 현재 프론트는 `String.includes` 단순 매칭이다.

---

## C등급 — 그 이후 (일부 해결)

### G-14
✅ **해결 (2026-08-06)** — [SPEC-07 API명세](../spec/SPEC-07_API명세.md).
`/api/v1/*` REST · RFC 9457 에러 · offset 페이지네이션 · httpOnly 쿠키 세션.
발행 게이트 실패는 `violations[]`로 **전부 한 번에** 반환한다 (`FR-ADMIN-003`).

정본은 Spring이 생성하는 OpenAPI이고 이 문서는 근거 기록이다 (`PRIN-T02`).

### G-15
✅ **해결 (2026-08-06)** — [SCREENS-06 어드민](../screens/SCREENS-06_어드민.md).

핵심은 **발행 조건 패널**이다. `FR-ADMIN-003`은 "실패 항목을 전부 한 번에"를 요구하지만,
더 나은 건 **누르기 전부터 보이는 것**이라 게이트를 편집 화면 우측에 상시 노출한다.
목록에도 `남은 N`을 띄워 마무리 우선 작업이 가능하게 했다.

입력 강제 — 3축은 **저장 자체가 안 되고**, 향·맛 태그는 4개째가 막히고,
재료는 **프리텍스트 입력란이 아예 없다**(`PRIN-D01`의 UI 구현).

### G-16
✅ **해결 (2026-08-06)** — [SPEC-04](../spec/SPEC-04_비기능요구사항.md).
`NFR-<분류>-nn` 체계로 성능·접근성·SEO·신뢰성·법적 기준을 **기준·측정·실패 시 조치** 셋으로 정의했다.

**토큰 대비를 실측한 것이 핵심이다.** accent는 3.76:1, `neutral-600`은 3.85:1로
둘 다 본문(4.5:1) 미달이었다.

그리고 **현재 코드가 실제로 위반하고 있었다** — 9~13px 텍스트에 두 토큰을 쓰는 곳이 18군데.
ADR-0001이 각주로 경고했지만 강제되지 않았다. `accent-700`(6.41:1) ·
`neutral-700`(5.83:1)로 교체하고, `NFR-A-01`을 **배포 차단** 조건으로 올렸다.

**남은 결정 하나** — `.btn-primary`가 3.76:1이다. `styles.css`는 시안 정본이라
임의로 못 고치므로 [SPEC-04 §2.4](../spec/SPEC-04_비기능요구사항.md)에 선택지 3안으로 남겼다.

### G-17
**PRD 16장 미결 10건** — 서비스명/도메인, 제휴 가격, 상권 우선순위, 에디터 채용 형태, 사진 예산,
재료 마스터 초기 목록, 연간 랭킹 시점 + v1.1에서 추가된 DB·CMS·호스팅.

이 중 DB·CMS는 [SPEC-05](../spec/SPEC-05_아키텍처.md)에서 확정됐다(PostgreSQL 16 · 자체 어드민).
**나머지는 사업 결정이라 스펙으로 풀리지 않는다.**

**진행 (2026-08-07)** — **에디터 채용 형태 해소**: 사용자 본인 + 주변인이 맡는다.
`GATE-COCKTAIL-01`이 막는 `tasting_note` 작성 담당이 확보됐다 ([이슈 036](https://github.com/cacaocoffee/k-cocktail-archive/issues/38)).
나머지 9건은 여전히 사업 결정이다.

---

## D등급 — Phase 1a 이슈 분해에서 발견 (2026-08-07)

> [`docs/issues/`](../issues/INDEX.md) 47개를 작성하며 스펙을 전수 대조한 결과다.
> SPEC-00 §4에 따라 등재한다 — **조용히 어기지 않는다.**

### G-18

✅ **해결 (2026-08-07)** — [TRACE-00 §4.1](../TRACE-00_추적매트릭스.md) 신설로 정정하고 G3 판정을 고쳤다.
대응: `FR-COCKTAIL-027`·`FR-USER-004`는 **API만 1a**(이슈 031), `FR-SEARCH-004`는 **SCREENS-03 문서화**(이슈 041).

**TRACE-00 §4·G3의 "Phase 1a 화면 명세가 닫혔다"가 사실과 다르다**

[TRACE-00 §3](../TRACE-00_추적매트릭스.md) 매트릭스는 아래 **Phase 1a P0 3건**을 미작성 화면 대기로 표시한다.

| FR | 우선순위 | 화면 | 상태 |
|---|---|---|---|
| `FR-COCKTAIL-027` 저장 · 공유 | **P0 (1a)** | SCREENS-04 마이 | ⬜ 미작성 |
| `FR-USER-004` 저장 · 컬렉션 · 공유 | **P0 (1a)** | SCREENS-04 마이 | ⬜ 미작성 |
| `FR-SEARCH-004` 파인더 도수 구간 | **P0 (1a)** | SCREENS-03 파인더 | ⬜ 미작성 |

그런데 §4 결론과 G3 판정은 이렇게 적혀 있다.

> **SCREENS-06 완료로 Phase 1a의 화면 명세가 닫혔다.** 남은 것은 전부 Phase 1b(바·파트너)이거나 **Phase 2(마이·파인더 확장)**다.
>
> | G3 · 화면 명세 | ◐ **Phase 1a 완료** (00·01·06) |

**SCREENS-03·04를 "Phase 2"로 분류했는데 거기 걸린 FR 3건이 1a P0다.**
범위의 정본은 [SPEC-01 §4.1](../spec/SPEC-01_시스템개요_범위.md)(USER 6건 = 소셜 로그인·저장·컬렉션 공유)이고,
TRACE-00은 스스로를 "구현 경로를 갖는지 확인하는 문서"라 규정했으므로 **검증 장치이지 범위 정의가 아니다.**

→ **TRACE-00의 결론이 틀렸다. G3은 통과가 아니다.**

**선택지**

| 안 | 내용 | 대가 |
|---|---|---|
| 가 | SCREENS-03·04 작성 | 착수가 늦어진다. 04는 화면이 아예 없어 새로 설계 |
| **나** | **저장·컬렉션은 API만 1a, 화면은 1b** | `FR-USER-004`의 "공유 링크 생성"까지 API로 충족. **파인더는 프로토타입 화면이 이미 있어 SCREENS-03을 "기존 화면 문서화"로 가볍게** |
| 다 | 해당 FR을 Phase 2로 내린다 | SPEC-01 §4.1 개정 — 범위 축소 |

**나안을 택했다** — [이슈 031](https://github.com/cacaocoffee/k-cocktail-archive/issues/33)을 API 범위로 축소하고 화면은 1b로 넘긴다.

### G-19

✅ **해결 (2026-08-07)** — SPEC-06에 `verification_task` · `category_intro` 추가.
`cocktail.flavor_profile`(5축)도 함께 — 프로토타입 데이터를 **이관 때 버리면 다시 만들 수 없다**.
TRACE-00 §3.5의 `FR-USER-002` ERD 칸에 `ingredient_brand.is_sponsored` 병기.

**SPEC-06에 테이블 2개가 없는데 FR이 요구한다**

| 테이블 | 요구하는 FR | 지금 SPEC-06 |
|---|---|---|
| **`verification_task`** | **`FR-ADMIN-004` 검증 태스크 큐 (P0)** | 없음 |
| `category_intro` | `FR-COCKTAIL-031` (P1) + **`NFR-S-07` 발행 차단** | 없음 |

`verification_task`가 더 위험하다. TRACE-00 §3.6은 ERD 칸에 `bar(hours_verified_at)`를 적었지만
**그건 태스크 발생 조건이지 큐 저장소가 아니다.** 큐를 구현하려면 테이블이 필요하다.
[SPEC-06 §4.3](../spec/SPEC-06_데이터모델_ERD.md)이 "위반 건을 관리자 태스크로 올린다"고 한 것도 저장소를 전제한다.

→ [이슈 016](https://github.com/cacaocoffee/k-cocktail-archive/issues/18)·[022](https://github.com/cacaocoffee/k-cocktail-archive/issues/24)가 만들되 **SPEC-06 개정이 선행**돼야 한다.

**부수 발견** — [TRACE-00 §3.5](../TRACE-00_추적매트릭스.md)의 `FR-USER-002` ERD 칸이 `article.is_sponsored` 하나뿐이다.
`article`은 Phase 2라 **1a에 경로가 없어 보인다.** FR 본문은 "`R-F1.3-3`의 광고성 구분 표기와 같은 지점에서 처리한다"고 했으므로
`ingredient_brand.is_sponsored`(1a)도 함께 적혀야 한다.

### G-20

✅ **해결 (2026-08-07)** — ① `FR-ADMIN-007` 문구를 "에디터가 요청하고 **관리자가 승인**"으로 개정.
② `NFR-S-07`을 **발행 차단 → 경고**로 내렸다 ([SPEC-04 §3.1](../spec/SPEC-04_비기능요구사항.md)) — P1이 P0 발행을 막던 구조를 풀었다.

**문서 간 충돌 2건**

**① 재료 승인 권한** — `FR-ADMIN-007`은 "**에디터** 승인 단계"라 하고
[SPEC-07 §2.2](../spec/SPEC-07_API명세.md)·[SPEC-08 §2](../spec/SPEC-08_보안_권한_개인정보.md)는 **`admin`** 이라 한다.

SPEC-07 §1.3이 "스코프 상세는 SPEC-08"이라고 **명시적으로 위임**했으므로 SPEC-08이 이긴다.
SPEC-08 §2.2의 권한 분리(중립성 장치) 논리와도 맞는다.
→ **`admin` 승인으로 구현**([이슈 026](https://github.com/cacaocoffee/k-cocktail-archive/issues/28)). **SPEC-03 문구를 고칠지 결정 필요.**

**② 카테고리 소개 문구** — `FR-COCKTAIL-031`은 **P1**인데 `NFR-S-07`은 **"발행 차단"**(배포 차단급)이다.
P1 기능이 P0 발행을 막는 구조다.
→ 보수적으로 **구조는 1a에 만들되 없어도 발행은 된다**로 구현. `NFR-S-07`의 차단은 P1 착수 시 적용
([이슈 022](https://github.com/cacaocoffee/k-cocktail-archive/issues/24)·[039](https://github.com/cacaocoffee/k-cocktail-archive/issues/41)). **둘 중 하나의 우선순위를 맞춰야 한다.**

### G-21

**이슈 분해가 스펙에 없는 테이블 2개를 도입했다 — 정당한지 확인 필요**

| 테이블 | 근거 | 판단 |
|---|---|---|
| `idempotency_key` | [SPEC-07 §1.7](../spec/SPEC-07_API명세.md)이 `Idempotency-Key` 요구 | 요구는 있고 **저장 방식이 미정**. DB/Redis 재량 |
| `batch_run` | **없음** | [SPEC-05 §8](../spec/SPEC-05_아키텍처.md)은 배치를 정의하나 **실행 이력을 요구하지 않는다.** 분해 과정에서 과하게 넣은 것일 수 있다 — **재검토** |

**해결 (2026-08-07)**

- **`batch_run` 제거** — 이슈 016에서 뺐다. **요구 없는 테이블을 만들지 않는다.**
  배치 실행 여부는 Spring Batch의 `BATCH_JOB_EXECUTION` 메타 테이블이 이미 남긴다(SPEC-05 §8이 Spring Batch를 지정).
  별도 이력이 필요해지면 그때 SPEC-05에 근거를 추가한다.
- **`idempotency_key` 유지** — SPEC-07 §1.7이 `Idempotency-Key`를 요구하고 저장소가 필요하다. 저장 방식(DB/Redis)은 구현 재량.

### G-22

**`NFR-L-05`("법률 검토 1회 = 정식 오픈 차단")가 과대 포장돼 있었다**

[SPEC-08 §8](../spec/SPEC-08_보안_권한_개인정보.md)의 검토 항목 3건이 **성격이 다른데 하나로 묶여** 있었다.
그 결과 "웹사이트 하나 만드는데 변호사가 필요한가"라는 인상을 줬다.

| 항목 | 성격 | 실제 트리거 |
|---|---|---|
| ① 전면 성인 인증 요구 여부 | 변호사 자문 | 접점 약함 — 판매를 안 한다 (ADR-0004) |
| ② 브랜디드 콘텐츠 주류광고 기준 | 변호사 자문 | **유료 브랜드 노출 시작 시** |
| ③ `audit_log` 파기 예외 적법성 | **문서 작성** | **실사용자 수집 전** — 처방침 문안의 일 |

**해결 (2026-08-07)** — [SPEC-04 §8.1](../spec/SPEC-04_비기능요구사항.md)에서 분해했다.

```
삭제:  NFR-L-05  법률 검토 1회        → 정식 오픈 차단
신설:  NFR-L-05  ①② 주류광고 자문      → 유료 브랜드 노출 시작 차단
개정:  NFR-L-04  처방침·약관 문안(③)   → 실사용자 수집 차단
```

**Phase 1a의 규제 접점은 0이다** — `article`(Ph2)·PARTNER(1b)가 없고 `ingredient_brand.is_sponsored`는 시드가 0건이다.
이슈 008·023이 "1a에서 켜지 않는다"를 DoD로 고정했다.

⚠️ **이 판단은 구조적 읽기이지 법률 판단이 아니다** — ADR-0004가 스스로 밝힌 것과 같은 한계다.

---

### G-23

**분류 축 2종의 표현이 프로토타입과 계약에서 다르다** *(전환 전 확인 완료)*

`PRIN-T02`가 Kotlin을 정본으로 세우면서 `packages/domain/src/types.ts`와 대조했다.
**멤버는 5개 축 전부 일치한다.** 다만 두 축의 **표현**이 다르고, 이슈 037이 그것을 옮긴다.

| 축 | 프로토타입 `types.ts` | 계약 (Kotlin → OpenAPI) | 왜 계약을 따르나 |
|---|---|---|---|
| `Technique` | `"Build" \| "Shake" \| …` PascalCase | `build` · `shake` · … 슬러그 | [SPEC-06 §3.1](../spec/SPEC-06_데이터모델_ERD.md)의 `method VARCHAR(12)`가 슬러그를 저장한다 |
| `SweetLevel` | `0 \| 1 \| 2 \| 3` 숫자 | `dry` · `semi_dry` · `semi_sweet` · `sweet` | 같은 문서의 `sweetness VARCHAR(12)` CHECK. **숫자는 의미가 위치에 숨어 마이그레이션에서 뒤집힌다** |

`SweetLevel`은 전환용으로 Kotlin enum이 `level: Int`를 함께 들고 있다.

**해결 (2026-08-10)** — 이슈 004가 `scripts/taxonomy-parity.mjs`를 만들어 대조를 자동화했다.
알려진 차이는 스크립트의 `KNOWN`에 근거와 함께 등록돼 있고, **목록에 없는 차이가 생기면 실패한다.**
`npm run check`에 포함된다.

> 전환 시점에 발견했다면 화면 넷을 동시에 고쳐야 했다. 계약을 먼저 세운 값어치가 여기 있다.

---

### G-25

**SPEC-06이 불리언 명명에서 스스로와 충돌한다** *(더 구체적인 쪽을 따랐다)*

| 어디 | 무엇 |
|---|---|
| [SPEC-06 §1.1](../spec/SPEC-06_데이터모델_ERD.md) | "불리언은 `is_` / `has_` 접두 (`is_signature`)" |
| **같은 문서 §3.1** `recipe_ingredient` | **`counts_for_stock`** `BOOLEAN NOT NULL DEFAULT true` |
| [SPEC-02 §2.7](../spec/SPEC-02_도메인모델.md) | 같은 이름 |

이슈 010의 스키마 린트(이슈 002)가 자기 새 컬럼을 잡으면서 드러났다.

**해결 (2026-08-11)** — **§3.1의 컬럼 정의를 따른다.** 이유가 셋이다.

1. 더 구체적인 규정이 일반 규칙을 이긴다 — §1.1은 관례고 §3.1은 그 컬럼의 정의다
2. 세 곳(SPEC-06 §3.1 · SPEC-02 §2.7 · 이슈 010)이 같은 이름을 쓴다
3. **§1.1의 취지를 이미 만족한다** — "이름만 보고 불리언인 줄 안다"가 목적인데
   `counts_for_stock`은 3인칭 동사구라 술어로 읽힌다. `is_`/`has_`가 유일한 술어 형태는 아니다

린트에 `PREDICATE_BOOLEANS` 예외로 근거와 함께 등록했다.
**억제가 쌓이는 것이 보이도록 코드 안에 둔다** — 늘어나면 §1.1을 고쳐야 한다는 신호다.

예외가 과하지 않은지 양쪽으로 확인했다. 픽스처의 `sponsored`는 계속 잡히고,
예외를 빼면 실제 스키마가 걸린다.

> `is_counted_for_stock`으로 바꾸는 쪽도 가능했다. 하지만 세 문서를 동시에 고쳐야 하고,
> `R-F2.2-5`가 이 이름으로 요구사항을 적었다 — **문서를 코드에 맞추는 방향**이라 택하지 않았다.

---

### G-24

**`INV-INGREDIENT-01`을 SPEC-06 §4.3은 "앱 강제"로 분류했지만 DB로도 강제한다** *(더 강한 방향)*

[SPEC-06 §4.3](../spec/SPEC-06_데이터모델_ERD.md)이 `INV-INGREDIENT-01`·`02`를
"조건부라 DB 제약으로 표현 불가"로 분류했다. `01`은 **표현 가능하다.**

```sql
CONSTRAINT ck_ingredient__substitute CHECK (
    domestic_availability NOT IN ('import_only','unavailable')
    OR (substitute_note IS NOT NULL AND substitute_note ~ '\S')
)
```

같은 문서 §4 서두가 **"DB로 강제할 수 있는 것은 DB에서 한다"**고 했으므로 §4.3의 분류보다
서두의 원칙이 우선한다고 보고 **양쪽 다** 걸었다 (`PRIN-T05`).

| 어디서 | 왜 |
|---|---|
| 앱 (`Ingredient.validate`) | 에디터에게 **어느 항목이 왜** 막혔는지 알려 준다. DB 제약 위반은 `violations`를 못 만든다 (`FR-ADMIN-003`) |
| DB (`CHECK`) | 배치·마이그레이션은 앱을 거치지 않는다 |

**해결 (2026-08-11)** — 이슈 008이 양쪽에 걸었다. 문서를 고치지 않은 이유는
**규칙이 더 강해진 방향**이라 스펙 위반이 아니기 때문이다. ADR까지는 만들지 않았다.

> ⚠️ **`INV-INGREDIENT-02`(브랜드 광고성 표기)는 여전히 앱 강제다.** "특정 브랜드 언급"의
> 판정이 데이터가 아니라 편집 판단이라 CHECK로 옮길 수 없다. `is_sponsored`가 `NOT NULL`인 것까지가
> DB가 할 수 있는 전부다.

구현 중 두 가지를 잡았다.

- **`trim()`은 공백만 지운다.** 탭·개행으로 채운 값이 통과했다. 코틀린 `isNullOrBlank()`는
  둘 다 잡으므로 앱이 DB보다 엄격해질 뻔했다 → `~ '\S'`로 바꿨다
- **`NULL ~ '\S'`는 `NULL`이고 CHECK는 `NULL`을 통과로 친다.** 안내를 아예 안 적은 경우가
  새어 나갔다 → `IS NOT NULL`을 명시했다 (3값 논리)

---

### G-26

**`audit_log`가 SPEC-06 §3.8·§4.1을 세 군데서 넘어선다** *(전부 더 강한 방향)*

이슈 014가 감사 로그를 만들며 문서 밖으로 나간 지점이다. 셋 다 `PRIN-T08`의
취지("되돌릴 수 있어야 하고, 다툼이 생겼을 때 근거가 돼야 한다")를 따라간 결과다.

| # | 문서 | 구현 | 왜 |
|---|---|---|---|
| 1 | §3.8 `action` 5종 | **8종** | SPEC-02 §8.1의 전이는 넷인데 표는 `publish`·`unpublish` 둘뿐이다. `archive`·`restore`를 넣지 않으면 전이의 절반이 기록되지 않는다 |
| 2 | — | `slug_change_attempt` **추가** | `NFR-D-04`가 "슬러그 변경 이력 0건, 발견 시 **즉시 조사**"를 요구한다. **거부된 시도**가 남지 않으면 조사할 대상이 영영 없다 |
| 3 | §4.1 보호 목록에 `audit_log` 없음 | **UPDATE·DELETE 회수** | 고칠 수 있는 이력은 이력이 아니다. §4.1은 `DELETE`만 다루지만 `UPDATE`로 고쳐도 결과는 같다 |

`archived → draft`를 `unpublish`가 아니라 `restore`로 나눈 이유: 사후 조사에서
"내렸다"와 "다시 꺼냈다"는 뜻이 다르다. 한 값으로 합치면 재구성(`NFR-O-05`)이 뭉개진다.

**`actor_user_id`에 FK를 걸지 않았다** — SPEC-08 §5.3이 "`user` 행 즉시 삭제"와
"`audit_log.actor_user_id` 유지"를 **동시에** 요구한다. FK가 있으면 둘 다 못 한다:
삭제가 FK 위반으로 막히거나, `ON DELETE`가 이력을 지운다. SPEC-06 §3.8의 컬럼 표도
FK를 명시하지 않았으므로 느슨한 참조로 갔다. **문서 충돌이라 스펙 쪽 정리가 필요하다.**

> ⚠️ `ViolationCode.INV_COCKTAIL_05`는 `HttpStatus.CONFLICT`로 선언돼 있지만
> `ApiExceptionHandler`가 `DomainViolationException`을 항상 `422`로 내보내 **그 값이 쓰이지 않는다.**
> 이슈 014의 RED 2가 요구한 것도 `422`라 지금 동작은 맞다. 다만 enum이 들고 있는 상태값이
> 죽은 정보라, 어드민 엔드포인트(이슈 025)가 붙을 때 정리한다.

---

### G-27

**규약 세 곳이 `packages/ui`의 수정 범위를 다르게 말한다** *(ADR-0005로 해소)*

외부 디자인 감사 스킬(hallmark)로 `apps/web`을 훑어 18건이 나왔다. **13건이 `packages/ui` 안**에 있었고,
착수하려니 어느 규약을 따라야 할지가 갈렸다.

| 출처 | `packages/ui/app.css` 를 뭐라고 하나 |
|---|---|
| [CONVENTIONS §4](../issues/CONVENTIONS.md) | `packages/ui` **통째로 수정 금지** |
| [`docs/design/README.md`](../design/README.md) | 「화면 레이아웃 · **반응형**」 층 |
| [ADR-0001](../decisions/ADR-0001-design-system.md) 결정 2 | `--dur`를 **일부러 `app.css`에 뒀다** — "시안 정본을 원본과 어긋나게 두지 않기 위해서" |

ADR-0001과 디자인 README는 `app.css`를 **우리가 쓴 층**으로 취급하는데 CONVENTIONS만 패키지 전체를 잠갔다.
반응형 수정은 `app.css`가 존재하는 이유 그 자체인데도 막힌다.

**해결 (2026-08-11)** — [ADR-0005](../decisions/ADR-0005-ui-package-scope.md).

```
docs/design/source/**      읽기 전용 — 시안 원본
packages/ui/styles.css     시안 정본 — GAPS → ADR → 되돌리는 조건, 3단 절차를 밟을 때만
packages/ui/app.css        프로젝트 소유 — 일반 코드와 같다
```

CONVENTIONS §4의 한 줄을 이 표로 교체했다. **`styles.css`의 3단 절차는 그대로 남는다** — 충돌 해소이지 완화가 아니다.

**함께 결정한 것 — 시안 정본 개정 3건.** ADR-0005 결정 3이 성격을 갈라 적었다.

| 무엇 | 성격 | 시안 원본 드리프트 |
|---|---|---|
| 램프 `hex` → `oklch()` | 표기 변경 (렌더 동일, `color-parity.mjs`가 강제) | 없음 |
| 한글 담는 칸 12px 바닥 | 접근성 — [G-16](#g-16)과 같은 원인(시안이 한글 미검증) | 있음 (부분) |
| **내비 마스트헤드 재구성** | **취향** — 근거는 외부 스킬의 의견이고 측정된 것이 아니다 | **있음** |

⚠️ **셋째는 시안이 의도한 형태를 덮는다.** 시안 원본 `K-Cocktail-Archive.dc.html:23-30`이
브랜드 좌측 + 번호 탭 3개 우측을 명시적으로 그렸고 현재 구현은 거기 충실했다.
제품 소유자 지시로 진행하되 **`styles.css`의 `.nav`는 건드리지 않아** 되돌리는 비용을 낮게 뒀다.
되돌리는 조건은 `cross_nav` 계측이다 (ADR-0005).

**이슈** — [#69](https://github.com/cacaocoffee/k-cocktail-archive/issues/69) 반응형 바닥 ·
[#70](https://github.com/cacaocoffee/k-cocktail-archive/issues/70) 토큰 규율 ·
[#71](https://github.com/cacaocoffee/k-cocktail-archive/issues/71) 아이브로우·내비 ·
[#72](https://github.com/cacaocoffee/k-cocktail-archive/issues/72) 램프·한글 바닥

> **남는 위험** — 형태 드리프트에는 자동 검증이 없다. 램프 값만 `color-parity.mjs`가 잡는다.
> 개정이 쌓이면 `docs/design/source/`는 점점 참조 문서가 아니게 된다.

---

### G-28

**이슈 023 의 "1안: `CocktailFacade` 경유 — 경계 준수" 는 틀렸다** *(경계 테스트가 잡았다)*

이슈 023(재료 사전)이 "이 재료를 쓰는 칵테일"(`R-F1.3-1`)의 모듈 경계를 두고 두 안을 적고 1안을 택했다.

> **1안**(`CocktailFacade`)을 택한다. SPEC-05 §3이 "조회 전용 서비스가 담당해 순환을 끊는다"고 했으나 **여기엔 순환이 없다.**

**순환이 있다.** [SPEC-05 §3](../spec/SPEC-05_아키텍처.md) 방향표에 `COCKTAIL ──uses──▶ INGREDIENT` 가 이미 있어, `ingredient → cocktail` 은 되돌아오는 화살표다.

놓친 구분이 이것이다 — **Facade 를 거치면 `repository` 직행만 막힌다. 모듈 간 화살표는 그대로 반대다.** `ModuleBoundaryTest` 의 `RED6`(방향표 위반)·`RED7`(순환)이 둘 다 걸렸다.

**해결 (2026-08-12)** — 같은 이슈가 적어 둔 **2안**으로 갔다. `SEARCH ──reads──▶ COCKTAIL · INGREDIENT` 가 방향표에 있고, `search` 는 여러 도메인을 읽으라고 있는 모듈이다. `GET /ingredients/{slug}/cocktails` 를 `kr.kcocktail.search.ingredient` 로 옮겼다.

> **URL 과 모듈은 별개다.** URL 은 사용자가 읽는 자원 구조이고 모듈은 코드의 의존 방향이다.
> `search/list/CocktailListController` 가 `/cocktails` 를 잡는 것과 같은 패턴이다.

**교훈**: 이슈 본문의 경계 판단은 방향표를 다시 대조해야 한다. 이 건은 사람이 리뷰로 잡으려면 SPEC-05 §3 을 외우고 있어야 했고, 경계 테스트가 자동으로 잡았다.

---

### G-29

**`FR-ADMIN-007`의 "에디터 승인"과 SPEC-08 §2의 "`admin` 승인"이 어긋난다** *(이슈 026)*

| 출처 | 문구 |
|---|---|
| [`FR-ADMIN-007`](../spec/SPEC-03_기능요구사항.md) | 재료 마스터 신규 추가는 **에디터 승인 단계**를 거친다 |
| `FR-INGREDIENT-001` | 신규 추가는 **에디터 승인제** |
| [SPEC-02 §3](../spec/SPEC-02_도메인모델.md) | 신규 추가는 에디터 승인제 |
| [SPEC-07 §2.2](../spec/SPEC-07_API명세.md) | `POST /admin/ingredients/{id}/approve` — 🔒 **`admin`** |
| [SPEC-08 §2](../spec/SPEC-08_보안_권한_개인정보.md) | 재료 마스터 승인 = **`admin`만** (`editor` ―) |

**결정 (2026-08-13) — `admin` 승인으로 간다.** 근거가 둘이다.

1. **SPEC-07 §1.3이 스코프 상세를 SPEC-08로 명시적으로 위임**했다. 위임한 쪽과 위임받은 쪽이 같은 말(`admin`)을 한다.
2. **SPEC-08 §2.2가 권한 분리를 중립성 장치로 규정**했다. 만드는 사람과 통과시키는 사람이 같으면 승인제가 형식만 남고, 마스터 오염을 막는다는 `PRIN-D01`의 전제가 자기 검토로 무너진다.

`FR-ADMIN-007`의 "에디터"는 **"에디터가 만들고 관리자가 승인"의 축약**으로 읽었다. 생성이 `editor`인 것은 양쪽이 일치하므로 문구 전체가 틀린 것은 아니다.

**남은 일**: SPEC-03 · SPEC-02 §3의 "에디터 승인제" 문구를 "에디터 요청 · 관리자 승인"으로 고칠지는 **제품 소유자 결정**이다. 구현은 SPEC-08을 따르되 조용히 맞추지 않고 여기 남긴다 (CONVENTIONS §6 · SPEC-00 §4).

**함께 처리한 것 두 가지.**

- **감사 확장** — 재료 승인을 `audit_log`에 남긴다 (`AuditAction.APPROVE`, `V023`). `PRIN-T08`이 열거한 4종에 없지만 [DECISIONS §1.3](../issues/DECISIONS.md)이 추가로 확정했다. 근거는 `PRIN-D01`이다 — 누가 통과시켰는지 없으면 오염을 되짚을 수 없다. [G-26](#g-26)의 `slug_change_attempt`와 같은 성격의 확장이다.
- **상한이 설정이 됐다** — `IngredientService.APPROVED_CAP` 상수를 `kcocktail.ingredient.approved-cap`으로 뺐다. `FR-INGREDIENT-001`의 "200~300개"는 범위이고 근거도 "역검색 UX"라는 관찰이다. 250인지 350인지는 목록을 띄워 봐야 알고, **경고 하나 조정하자고 배포하지 않는다.**

> **덤으로 발견한 결함** — 매핑이 없는 경로가 404가 아니라 **500**이었다. 부트 3.2부터 이 경우는 `NoHandlerFoundException`이 아니라 `NoResourceFoundException`으로 오는데 `ApiExceptionHandler`가 옛 예외만 걸고 있어 catch-all이 받았다. 상태가 틀린 것보다 나쁜 게 따라온다 — 없는 경로를 긁는 봇 하나가 `ERROR` 로그를 채워 진짜 500을 묻는다. `RestConventionTest`에 회귀 테스트를 세웠다.

---

### G-30

**SPEC-05 §3 방향표에 `USER ──reads──▶ COCKTAIL` 을 추가했다** *(이슈 031)*

북마크는 [SPEC-05 §3](../spec/SPEC-05_아키텍처.md)이 `USER`에 배정한 것인데(`USER ──owns──▶ STOCK · Bookmark`), 표에는 `USER`가 `COCKTAIL`을 읽을 화살표가 없었다. 그런데 북마크는 본질적으로 **콘텐츠를 가리킨다**:

| 시점 | 왜 대상을 읽어야 하나 | RED |
|---|---|---|
| 저장 | 발행된 것만 저장한다 — `draft`를 저장할 수 있으면 슬러그를 찔러 보는 것만으로 발행 전 콘텐츠의 존재가 샌다 | 5 · 28 |
| 조회 | 무엇을 저장했는지 보여준다 + 사라진 것을 거른다 | 25 · 26 · 27 |

**가리키는 대상을 못 읽는 북마크는 쓸모가 없다.**

**검토했다 버린 안** — `SEARCH` 경유. `SEARCH ──reads──▶ COCKTAIL`이 이미 있지만 그쪽은 `USER`를 읽지 못한다(표에 없다). 북마크 행을 읽으려면 반대 화살표가 필요해져 **문제가 옮겨갈 뿐**이다.

**해결 (2026-08-13)** — 코드가 아니라 **SPEC-05 §3을 먼저 고쳤다** (SPEC-00 §4 · `ModuleBoundaryTest`의 KDoc이 명시한 순서). 순환은 생기지 않는다 — `COCKTAIL`은 `USER`를 참조하지 않는다. `user`의 팬아웃은 `stock` + `cocktail` = 2로 표의 상한(CONTENT의 2) 안이다.

> **[G-28](#g-28)에서 배운 것이 여기서 값을 했다.** 이슈 031 본문의 GREEN도 "`CocktailFacade` 경유 (모듈 경계)"라고 적어, 023과 **똑같은 오해**를 하고 있었다 — Facade를 거치면 `repository` 직행만 막히고 **모듈 화살표는 그대로 반대**다. 023 때는 경계 테스트가 잡았고, 이번엔 착수 전에 알아봤다.

**덤으로 발견한 결함 — `/me`가 공유 캐시에 올라가고 있었다.** `CacheControlFilter`·`PublicEtagFilter`가 `ApiPaths.isPublicApi`로 판정했는데, 그 함수는 "`/api/v1` 아래이고 `/admin`이 아닌 것"이라 `/me/bookmarks`가 통과했다. `Cache-Control: public, max-age=60`과 ETag가 붙어 나갔고, **중간 캐시가 한 사람의 북마크를 다른 사람에게 줄 수 있다**는 뜻이다. `/auth/csrf`도 같이 걸렸다 — 토큰이 60초 캐시되면 세션 바인딩의 의미가 없다.

"공개 API인가"와 "공유 캐시에 올려도 되는가"는 다른 질문이라 `isPubliclyCacheable`로 나눴다. 인증 경로를 열거하지 않고 접두사(`/me`·`/auth`)로 판정한다 — 열거하면 새 개인 경로가 목록에서 빠진 채 들어오고, **그때 새는 것은 조용하다.** `RestConventionTest`에 회귀를 세웠다.

> 이 결함은 이슈 031이 **`/api/v1` 아래 첫 개인 엔드포인트**라서 드러났다. 그 전까지는 공개 아니면 어드민 둘뿐이었다.

---

### G-31

**홈(`/`)에 이슈가 없다 — 탐색 화면이 그 자리를 쓰고 있었다** *(이슈 040)*

[SPEC-05 §4](../spec/SPEC-05_아키텍처.md)의 렌더링 표는 두 경로를 나눠 적었다.

| 경로 | 방식 | 색인 |
|---|---|---|
| `/` 홈 | ISR 10분 | ✅ |
| `/cocktails/search?…` | 클라이언트 필터 | ❌ `noindex` |

프로토타입은 탐색 화면을 `/` 에 두고 있었다. 그대로 두면 **색인 대상 경로에 `noindex` 화면**이
있는 셈이고, `NFR-S-02`(필터 결과 `noindex`)가 배포 차단 조건이라 그렇게 둘 수 없다.

이슈 040 이 화면을 `/cocktails/search` 로 옮기고 `/` 는 **임시 이동(307)** 으로 남겼다.
영구 이동이 아닌 것이 중요하다 — 홈이 생기면 그 줄을 지우고 페이지를 놓으면 된다.

이동은 `next.config.ts` 의 `redirects()` 가 한다 (이슈 042 에서 옮겼다). 페이지에서
`redirect()` 를 부르면 `/` 가 정적 페이지로 미리 그려지고 이동이 **브라우저에서** 일어나,
화면이 한 번 그려졌다 사라지는 사이에 실행 중이던 것이 문맥을 잃는다.

**남은 일**: 홈 화면을 만드는 이슈가 [INDEX](../issues/INDEX.md) 어느 웨이브에도 없다.
사이트맵은 `/` 를 색인 대상으로 계속 내보내고 있으므로(이슈 039), 홈이 생기기 전까지
그 항목은 **탐색으로 보내는 주소**다. 홈이 무엇을 담을지는 제품 결정이라 여기 남긴다.

### G-32

**메이킹 축의 한국어 이름이 두 벌이다** *(이슈 040 에서 드러남)*

| 어디 | `stir` | `build` |
|---|---|---|
| 계약 `x-labels` → `generated/labels.ts` | **스터** | **빌드** |
| `packages/domain/src/data.ts` 의 `TECHNIQUES` | **휘저어 섞기** | **잔에서 조립** |

둘 다 의도된 것이다 — [`types.ts`](../../packages/domain/src/types.ts) 가 "분류 축의 한국어 이름은
계약, **화면 표시 문구**는 `data.ts`" 라고 나눴다. 문제는 **같은 화면이 출처에 따라 다른 이름을
보여 준다**는 것이다: 카테고리 페이지는 API 가 붙으면 `스터`, 프로토타입이면 `휘저어 섞기` 다
(`KC_API_URL` 을 주고 `apps/web/e2e/category.spec.ts` 를 돌리면 그 자리에서 깨진다).

이슈 040 의 필터 칩은 `TECHNIQUES` 쪽을 쓴다 — 칩은 "무엇을 하는지" 를 말해야 하고
`스터`·`빌드`는 그 자체로는 뜻이 통하지 않는다.

**남은 일**: 셋 중 하나다. ① 계약의 `x-labels` 를 풀어 쓴 말로 바꾼다(그러면 `data.ts` 의
표시 문구가 필요 없어진다) ② 화면이 항상 `data.ts` 를 쓴다(카테고리 페이지가 API 레이블을
버린다) ③ 지금처럼 둔다. **조용히 맞추지 않고 여기 남긴다** (SPEC-00 §4).

### G-33

**파인더(`/finder`)를 색인할지가 정해져 있지 않았다** *(이슈 041에서 결정)*

[SPEC-05 §4](../spec/SPEC-05_아키텍처.md)의 렌더링 표에 `/finder`가 **없다.** 색인 여부도
`noindex` 여부도 적힌 곳이 없었는데, 이슈 041이 답과 단계를 쿼리스트링에 실으면서
(`FR-SEARCH-005`와 같은 이유 — 공유 가능해야 한다) 조합마다 주소가 생겼다.

| 안 | 대가 |
|---|---|
| 전부 색인 | `?abv=high&sweet=dry&…` 조합이 같은 내용으로 색인된다. `PRIN-P06`이 막으려는 중복 콘텐츠 |
| **`noindex`** | 파인더가 검색에서 사라진다. 기능 소개가 있는 진입 화면이고 사이트맵에도 있다 (이슈 039) |
| **canonical 고정** ← 택함 | 진입 화면은 색인되고 답이 붙은 주소는 `/finder`로 합쳐진다 |

**canonical을 답 없는 `/finder`로 고정했다.** 답이 붙은 주소는 같은 화면의 다른 상태이지
다른 문서가 아니다 — 탐색 필터가 `noindex`인 것과 성격이 다르다. 저쪽은 결과 목록 자체가
색인 가치를 갖지 않지만, 파인더는 **주소와 무관하게 같은 질문 화면**이다.

**남은 일**: SPEC-05 §4 표에 `/finder` 줄을 추가할지는 문서 소유자 결정이다. 표는
"경로마다 렌더링 방식이 정해져 있다. 임의로 바꾸지 않는다"(`PRIN-T04`)고 했는데 이 경로만
표 밖에 있다. 구현은 **셸 정적 + 클라이언트 진행**으로 두고 여기 남긴다 (SPEC-00 §4).

### G-34

**시드로 들어온 41종이 검색 색인에 없다** *(이슈 042 에서 드러남)*

`search_document` 는 **발행 이벤트**를 듣고 채워진다 (`SearchIndexListener` · 이슈 017).
그런데 코퍼스는 SQL 시드(`R__seed_02_cocktail.sql` · 이슈 036)로 들어왔다 — 애플리케이션을
거치지 않으므로 이벤트가 없고, 색인 행도 없다.

실측 (2026-08-18, 로컬):

```
select count(*) from cocktail where status='published';   →  41
select count(*) from search_document;                      →   0
GET /api/v1/search?q=네그로니  →  matchedCount: 0
```

**통합 검색이 발행분 전부를 못 찾는다.** 화면·API·색인 코드는 각각 맞는데 셋을 이어 보면
비어 있는 상태라, 어느 한 이슈의 테스트로는 드러나지 않았다.

**남은 일**: 시드 경로에 색인을 채우는 방법을 정해야 한다. 세 가지가 보인다.

| 안 | 성격 |
|---|---|
| 시드 SQL 이 `search_document` 도 넣는다 | 색인 규칙(초성 분해)이 SQL 에 복제된다 — `Chosung` 이 두 벌이 된다 |
| **재색인 배치·CLI** | 이슈 016 의 검증 배치와 같은 자리. 운영에서도 필요하다 (색인이 깨졌을 때 되살릴 길) |
| 시드를 API 로 넣는다 | 이벤트가 자연히 발생한다. 대신 시드가 앱 기동에 묶인다 |

이슈 036(시드)·017(색인) 어느 쪽 소유인지도 함께 정한다. **이슈 042 는 화면이라
여기서 고치지 않는다** (SPEC-00 §4 — 조용히 맞추지 않는다).

### G-35

**계량 단위 5종 중 `ml` 하나만 쓰이고 있다** *(이슈 043 에서 확인)*

`FR-COCKTAIL-019`·`020` 은 `dash`·`barspoon`·`piece`·`top_up` 을 배수·변환에서 뺀다고 했고
이슈 043 이 그 규칙을 구현했다. 그런데 **지금 데이터에는 그 단위가 한 행도 없다.**
발행 41종을 전수 조회한 결과 `unit` 은 전부 `ml` 이고, 나머지는 `amount_label` 이다.

| 라벨 (16종) | 수 |
|---|---|
| `1조각` · `1조각 (선택)` | 17 |
| `2 dash` · `1~2 dash` · `각 2 dash` · `1 dash (8~10 drops)` | 6 |
| `3~4 barspoon` · `10장` · `8장` · `1줄기` · `림 절반` · `1개` · `반 개 …` · `적당량 …` | 나머지 |

**이슈 043 RED 4 의 모호함(`2 dash` 가 라벨인가 수량인가)은 데이터가 답했다** — 라벨이다.
그래서 규칙대로 배수를 타지 않는다. 다만 그 결과로 **"2 dash × 2잔 = 4 dash" 를 사용자가
볼 수 없다.** 두 잔을 만드는 사람은 비터스를 두 배로 넣어야 하는데 화면은 `2 dash` 그대로다.

**한 건은 분명한 데이터 결함이다** — 위스키 사워의 달걀 흰자가 `amount_label = '15ml'` 다.
`ml` 이면서 라벨이라 배수도 oz 변환도 걸리지 않는다.

**남은 일**: 시드(이슈 036)가 라벨로 밀어 넣은 것 중 **수량+단위로 적을 수 있는 것**을
가릴지 정해야 한다. `1조각`·`림 절반`은 라벨이 맞고, `2 dash`·`3~4 barspoon`·`15ml` 는
`amount`+`unit` 으로 적을 수 있다(`1~2`·`3~4` 는 범위라 다시 정해야 한다).
**화면 이슈에서 데이터를 고치지 않는다** — 규칙은 양쪽(라벨·수량) 모두 구현해 두었으므로
데이터가 바뀌면 그날 바로 동작한다.

---

## 다음 할 일

DB · CMS · 백엔드 스택이 확정되면서 SPEC-05·06·07이 나왔고, 병목이 다시 옮겨갔다.
남은 6건 중 넷은 **문서를 쓰면 풀리는 것**이고, 호스팅·이미지 저장소만 사업 결정이다.

권장 순서:

1. **호스팅 · 이미지 저장소 결정** *(사업/인프라)* ([G-10](#g-10)) — `analytics_event` 테이블은 잡혔지만 이벤트 정의가 없다.
   코드가 커지기 전에 심는 게 싸고, `R-F4.3-2`가 이 데이터에 의존한다
2. **SCREENS-00~06** ([G-09](#g-09) · [G-15](#g-15)) — 빈 상태·에러·권한 분기
3. **SPEC-04 NFR** ([G-16](#g-16)) — accent 대비 3:1 제약 명문화 포함

**호스팅 · 이미지 저장소**는 위와 병행해 결정한다. 스펙을 더 써도 풀리지 않는 항목이다.
